<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="FortniteLauncher.Pages.MainShellPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FortniteLauncher.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="PageRoot">
        <NavigationView x:Name="MainNavigation"
                        Loaded="MainNavigation_Loaded"
                        PaneDisplayMode="Left"
                        IsSettingsVisible="False"
                        IsBackButtonVisible="Collapsed"
                        IsPaneToggleButtonVisible="False"
                        AlwaysShowHeader="True"
                        IsTitleBarAutoPaddingEnabled="False"
                        OpenPaneLength="52"
                        IsPaneOpen="True"
                        CompactModeThresholdWidth="0"
                        ExpandedModeThresholdWidth="0"
                        Background="#202336"
                        SelectionChanged="MainNavigation_SelectionChanged">

            <NavigationView.Resources>
                <Thickness x:Key="NavigationViewHeaderMargin">56,26,0,0</Thickness>
                <SolidColorBrush x:Key="NavigationViewContentGridBorderBrush" Color="{StaticResource NavigationViewExpandedPaneBackground}"/>
                <SolidColorBrush x:Key="NavigationViewContentBackground" Color="{StaticResource NavigationViewExpandedPaneBackground}"/>
            </NavigationView.Resources>

            <NavigationView.Header>
                <BreadcrumbBar x:Name="MainBreadcrumb"
                               FontSize="48"
                               FontWeight="SemiBold"
                               ItemClicked="MainBreadcrumb_ItemClicked">
                    <BreadcrumbBar.Resources>
                        <Style x:Key="BreadcrumbBarItemStyle" TargetType="BreadcrumbBarItem">
                            <Setter Property="FontSize" Value="28"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Foreground" Value="{ThemeResource TextFillColorTertiaryBrush}"/>
                        </Style>
                        <x:Double x:Key="BreadcrumbBarChevronFontSize">12</x:Double>
                        <Thickness x:Key="BreadcrumbBarChevronPadding">12,0</Thickness>
                        <ThemeResource x:Key="BreadcrumbBarNormalForegroundBrush" ResourceKey="TextFillColorSecondaryBrush"/>
                        <ThemeResource x:Key="BreadcrumbBarHoverForegroundBrush" ResourceKey="TextFillColorTertiaryBrush"/>
                    </BreadcrumbBar.Resources>
                    <BreadcrumbBar.ItemTemplate>
                        <DataTemplate x:Name="BreadcrumbBarItemTemplate" x:DataType="BreadcrumbBarItem">
                            <BreadcrumbBarItem Style="{StaticResource BreadcrumbBarItemStyle}"
                                               Content="{Binding Content}">
                                <BreadcrumbBarItem.ContentTransitions>
                                    <TransitionCollection>
                                        <ContentThemeTransition/>
                                    </TransitionCollection>
                                </BreadcrumbBarItem.ContentTransitions>
                            </BreadcrumbBarItem>
                        </DataTemplate>
                    </BreadcrumbBar.ItemTemplate>
                    <BreadcrumbBar.Transitions>
                        <TransitionCollection>
                            <ContentThemeTransition/>
                        </TransitionCollection>
                    </BreadcrumbBar.Transitions>
                </BreadcrumbBar>
            </NavigationView.Header>

            <NavigationView.MenuItems>
                <NavigationViewItem x:Name="PlayPageItem">
                    <NavigationViewItem.Icon>
                        <ImageIcon Width="24" Height="24" Margin="-4">
                            <ImageIcon.Source>
                                <SvgImageSource UriSource="ms-appx:///Content/Texture/Icons/IC_Play.svg"/>
                            </ImageIcon.Source>
                        </ImageIcon>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem x:Name="DownloadsItem">
                    <NavigationViewItem.Icon>
                        <ImageIcon Width="24" Height="24" Margin="-4">
                            <ImageIcon.Source>
                                <SvgImageSource UriSource="ms-appx:///Content/Texture/Icons/IC_Download.svg"/>
                            </ImageIcon.Source>
                        </ImageIcon>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem x:Name="ItemShopItem"
                                    IsEnabled="False"
                                    ToolTipService.ToolTip="Shop Services are currently unavailable">
                    <NavigationViewItem.Icon>
                        <ImageIcon Width="24" Height="24" Margin="-4">
                            <ImageIcon.Source>
                                <SvgImageSource UriSource="ms-appx:///Content/Texture/Icons/IC_Shop.svg"/>
                            </ImageIcon.Source>
                        </ImageIcon>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem x:Name="LeaderboardItem">
                    <NavigationViewItem.Icon>
                        <ImageIcon Width="24" Height="24" Margin="-4">
                            <ImageIcon.Source>
                                <SvgImageSource UriSource="ms-appx:///Content/Texture/Icons/IC_Leaderboard.svg"/>
                            </ImageIcon.Source>
                        </ImageIcon>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>

                <NavigationViewItem x:Name="ServerStatusItem">
                    <NavigationViewItem.Icon>
                        <ImageIcon Width="24" Height="24" Margin="-4">
                            <ImageIcon.Source>
                                <SvgImageSource UriSource="ms-appx:///Content/Texture/Icons/IC_ServerStatus.svg"/>
                            </ImageIcon.Source>
                        </ImageIcon>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>

            <NavigationView.FooterMenuItems>
                <NavigationViewItem x:Name="SettingsItem">
                    <NavigationViewItem.Icon>
                        <ImageIcon Width="24" Height="24" Margin="-4">
                            <ImageIcon.Source>
                                <SvgImageSource UriSource="ms-appx:///Content/Texture/Icons/IC_Settings.svg"/>
                            </ImageIcon.Source>
                        </ImageIcon>
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.FooterMenuItems>

            <Frame x:Name="RootFrame" Margin="0,0,0,0"/>
        </NavigationView>

        <!-- Disabled controls do not receive pointer input, so this transparent overlay preserves the unavailable tooltip. -->
        <Canvas IsHitTestVisible="True">
            <Border x:Name="ShopServiceUnavailableOverlay"
                    Background="Transparent"
                    ToolTipService.ToolTip="Shop Services are currently unavailable"
                    ToolTipService.Placement="Right"
                    PointerPressed="ShopServiceUnavailableOverlay_PointerPressed"/>
        </Canvas>
    </Grid>
</Page>
